"""
Just a scratch code to see how non-parametric dynamic programming works.
susami@andrew.cmu.edu
"""

class KernelDensityEstimate():
    def __init__(self):
        """
        holds samples for when predicting the
        """
        self.samples = samples
        self.bandwidth = bandwidth



def generate_samples(n_samples)
    """ tp1 means t+1 """

    samples = []
    for i in range(n_samples)

        n_samples.append()

    return s_t, s_tp1, r


def main():
    n_samples = 100
    samp_s_t, samp_s_tp1, samp_r = generate_samples(100)


main()
